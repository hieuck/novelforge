interface:
  display_name: "Novelforge"
  short_description: "Repo-specific patterns and workflows for novelforge"
  default_prompt: "Use the novelforge repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true
